[project]
name = "cim-lab-0"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "hydra-core>=1.3.7",
    "matplotlib>=3.11.2",
    "torch>=2.14.0",
    "torchvision>=0.29.0",
    "wandb>=0.30.0",
]

[project.scripts]
cim-lab-0 = "cim_lab_0:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
